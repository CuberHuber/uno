# Beta Core (Engine 2.0 + Rooms) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement sub-project A of the beta spec — four host-toggleable rules (strict stacking, force play, draw-to-match, stack discard) with a bilingual rules catalog, 5-character room codes, an optional 4-digit room PIN, rate limiting, and a ru/en client.

**Architecture:** The engine stays a pure reducer with per-rule conditionals; rule prose lives in a new declarative catalog in `shared/`. Server error strings become stable snake_case codes rendered by client dictionaries. Protocol changes ride the existing Socket.IO events plus a widened `POST /api/rooms`.

**Tech Stack:** TypeScript monorepo (npm workspaces), Node + Fastify 5 + Socket.IO 4, React 19 + Vite, vitest (shared + server only).

**Spec:** `docs/superpowers/specs/2026-08-12-beta-core-design.md` (approved).

## Global Constraints

- All four rules default **off**; with all off, behavior is exactly classic — the pre-existing suite must stay green (only mechanical churn allowed: `cardIds` arrays, `cards` arrays in effects, error codes).
- Room code alphabet is exactly `34679ACDEFHKMNPRTWXY` (20 symbols), length 5, case-insensitive input, spaces/dashes stripped.
- PIN is exactly `/^\d{4}$/`, optional, stored as plain text in room memory (spec'd decision).
- Rule prose (title/tagline/details, ru+en) lives ONLY in `shared/src/rulesCatalog.ts`. UI strings live ONLY in `client/src/i18n.tsx`.
- Server rejection reasons are snake_case codes (table in Task 2); never send prose.
- Dev machine runs Node 18: no `import.meta.dirname`, no Node-20-only APIs in server code.
- Do not start a dev server on :3000 — the user runs `npm start -w server` themselves; note that `@fastify/static` snapshots `server/public` at boot, so they must restart after a rebuild.
- Visuals follow `design/Ochre Eights - Full Game.dc.html` and `design/Ochre Eights - Card Set.dc.html` — reuse their patterns/classes; do not invent a new visual language.
- Client has no test runner: client tasks verify with `npm run typecheck && npm run build` from the repo root.
- Commit after every task (conventional commits). Work on branch `worktree-beta-core-spec`.
- Run tests from the repo root: `npm test` (runs shared + server). Single file: `npx vitest run test/<file> -r server`.

---

### Task 1: Rules type + bilingual rules catalog

**Files:**
- Modify: `shared/src/types.ts:14-18` (Rules), `shared/src/index.ts`
- Modify: `server/src/rooms.ts:104` (sanitize), `client/src/ui.ts:4-7` (RULE_DEFS)
- Create: `shared/src/rulesCatalog.ts`
- Test: `shared/test/rulesCatalog.test.ts`

**Interfaces:**
- Consumes: existing `Rules`, `CLASSIC_RULES` from `shared/src/types.ts`.
- Produces: `Rules` with four booleans (`stacking`, `forcePlay`, `drawToMatch`, `multiDiscard`); `sanitizeRules(r?: Partial<Rules>): Rules` exported from `shared`; `RULES_CATALOG: readonly RuleInfo[]` and `interface RuleInfo { id: keyof Rules; title/tagline/details: { ru: string; en: string }; default: boolean }` exported from `shared`.

- [ ] **Step 1: Write the failing test**

```ts
// shared/test/rulesCatalog.test.ts
import { describe, expect, test } from 'vitest';
import { CLASSIC_RULES, RULES_CATALOG, sanitizeRules } from '../src/index.js';

describe('rules catalog', () => {
  test('covers every Rules key exactly once, defaults off', () => {
    const ids = RULES_CATALOG.map((r) => r.id).sort();
    expect(ids).toEqual(['drawToMatch', 'forcePlay', 'multiDiscard', 'stacking']);
    expect(RULES_CATALOG.every((r) => r.default === false)).toBe(true);
    expect(Object.keys(CLASSIC_RULES).sort()).toEqual(ids);
  });
  test('carries non-empty ru and en prose for every rule', () => {
    for (const r of RULES_CATALOG) {
      for (const loc of ['ru', 'en'] as const) {
        expect(r.title[loc].length).toBeGreaterThan(0);
        expect(r.tagline[loc].length).toBeGreaterThan(0);
        expect(r.details[loc].length).toBeGreaterThan(20);
      }
    }
  });
  test('sanitizeRules coerces partial junk to four booleans', () => {
    expect(sanitizeRules()).toEqual(CLASSIC_RULES);
    expect(sanitizeRules({ stacking: true })).toEqual({ ...CLASSIC_RULES, stacking: true });
  });
});
```

- [ ] **Step 2: Run it — expect FAIL** (`RULES_CATALOG` not exported): `npx vitest run -r shared`

- [ ] **Step 3: Implement**

In `shared/src/types.ts` replace the `Rules` block:

```ts
export interface Rules {
  stacking: boolean;     // +2 answers only +2, +4 answers only +4; the pot rides on
  forcePlay: boolean;    // a drawn playable card goes straight down
  drawToMatch: boolean;  // no play → draw until a playable card arrives
  multiDiscard: boolean; // same-value number cards may be discarded together
}
export const CLASSIC_RULES: Rules = {
  stacking: false, forcePlay: false, drawToMatch: false, multiDiscard: false,
};
export const sanitizeRules = (r?: Partial<Rules> | null): Rules => ({
  stacking: !!r?.stacking, forcePlay: !!r?.forcePlay,
  drawToMatch: !!r?.drawToMatch, multiDiscard: !!r?.multiDiscard,
});
```

Create `shared/src/rulesCatalog.ts` (full prose — this is the single source):

```ts
import type { Rules } from './types.js';

export interface RuleInfo {
  id: keyof Rules;
  title: { ru: string; en: string };
  tagline: { ru: string; en: string };
  details: { ru: string; en: string };
  default: boolean;
}

export const RULES_CATALOG: readonly RuleInfo[] = [
  {
    id: 'stacking',
    title: { ru: 'Перевод штрафа', en: 'Pass the penalty' },
    tagline: {
      ru: '+2 отбивается только +2, +4 — только +4; штраф едет дальше.',
      en: '+2 answers +2, +4 answers +4 — the pot rides on.',
    },
    details: {
      ru: 'Получив +2, можно не брать карты, а ответить своей +2 — тогда следующий '
        + 'игрок должен уже +4. Пот от +4 отбивается только другой +4. Цвет карты-ответа '
        + 'не важен. Кто не может или не хочет ответить — берёт весь накопленный штраф '
        + 'и пропускает ход.',
      en: 'When a +2 lands on you, you may answer with your own +2 instead of drawing — '
        + 'the next player then owes +4. A +4 pot can only be answered with another +4. '
        + 'The answer card’s color does not matter. Whoever cannot (or will not) answer '
        + 'draws the whole pot and misses their turn.',
    },
    default: false,
  },
  {
    id: 'forcePlay',
    title: { ru: 'Форс-плей', en: 'Force play' },
    tagline: {
      ru: 'Вытянутая играбельная карта сразу ложится.',
      en: 'A drawn playable card goes straight down.',
    },
    details: {
      ru: 'Если карта, которую ты вытянул, подходит — она сразу идёт в сброс, придержать '
        + 'её нельзя. Дикие карты ждут, пока ты выберешь цвет.',
      en: 'If the card you draw can be played, it is played immediately — no keeping it '
        + 'for later. Wilds still wait for you to pick a color.',
    },
    default: false,
  },
  {
    id: 'drawToMatch',
    title: { ru: 'Тяни до совпадения', en: 'Draw to match' },
    tagline: {
      ru: 'Нечем ходить — тяни, пока не придёт подходящая.',
      en: 'No play? Draw until something plays.',
    },
    details: {
      ru: 'Когда нечем ходить, ты тянешь не одну карту, а до первой играбельной. '
        + 'Штрафы +2/+4 не меняются — там берётся ровно столько, сколько написано. '
        + 'Если колода кончилась, ход переходит дальше.',
      en: 'When you cannot (or will not) play, you do not draw just one card — you keep '
        + 'drawing until you hit a playable one. Penalties (+2/+4) are unaffected: those '
        + 'are exact counts. If the deck runs dry, your turn passes.',
    },
    default: false,
  },
  {
    id: 'multiDiscard',
    title: { ru: 'Сброс стопкой', en: 'Stack discard' },
    tagline: {
      ru: 'Один номинал, любые цвета — сбрось разом.',
      en: 'Same number, any colors — throw them together.',
    },
    details: {
      ru: 'За один ход можно сбросить несколько числовых карт одного номинала — '
        + 'например, синюю 5, красную 5 и зелёную 5. Первая должна подходить по обычным '
        + 'правилам; последняя сброшенная задаёт цвет. Карты действий стопкой не сбрасываются.',
      en: 'You may discard several number cards of the same value in one turn — say, a '
        + 'blue 5 with a red 5 and a green 5. The first must be playable as usual; the '
        + 'last one thrown sets the color. Action cards cannot be stacked.',
    },
    default: false,
  },
];
```

Add to `shared/src/index.ts`: `export * from './rulesCatalog.js';`

Keep the build green:
- `server/src/rooms.ts:104` — replace the two-field literal with
  `room.rules = sanitizeRules(rules);` (import `sanitizeRules` from `@uno/shared`).
- `client/src/ui.ts` — extend `RULE_DEFS` to four entries as an interim stopgap
  (Tasks 10/12 replace it with the catalog + locale):

```ts
export const RULE_DEFS: readonly { key: keyof Rules; name: string; desc: string }[] = [
  { key: 'stacking', name: 'Stacking +2 / +4', desc: 'Pass the penalty along instead of drawing it.' },
  { key: 'forcePlay', name: 'Force play', desc: 'If a drawn card is playable, it goes down at once.' },
  { key: 'drawToMatch', name: 'Draw to match', desc: 'No play? Draw until something plays.' },
  { key: 'multiDiscard', name: 'Stack discard', desc: 'Same number, any colors — throw them together.' },
];
```

- [ ] **Step 4: Run tests + typecheck — expect PASS**: `npm test && npm run typecheck`

- [ ] **Step 5: Commit**

```bash
git add shared client/src/ui.ts server/src/rooms.ts
git commit -m "feat: four-flag Rules with bilingual rules catalog in shared"
```

---

### Task 2: Server error strings become stable codes

**Files:**
- Modify: `server/src/engine/game.ts` (every `err('…')`), `server/src/rooms.ts` (every `error: '…'`)
- Test: existing `server/test/*.test.ts` (assertions on error text, if any)

**Interfaces:**
- Produces: the canonical error-code table below. All later tasks (and client dictionaries in Task 9) use exactly these codes.

| Old prose | Code |
|---|---|
| round is over | `round_over` |
| bad seat | `bad_seat` |
| no color choice pending | `no_color_pending` |
| play the drawn card or pass | `play_drawn_or_pass` |
| card not in hand | `card_not_in_hand` |
| answer the +N or draw | `answer_pot` |
| card does not match | `card_no_match` |
| wild needs a color | `wild_needs_color` |
| not your turn | `not_your_turn` |
| choose a color first | `choose_color_first` |
| nothing to pass | `nothing_to_pass` |
| force play — the drawn card goes down | `force_play` |
| cannot call now | `cannot_call_now` |
| nothing to catch | `nothing_to_catch` |
| cannot catch yourself | `cannot_catch_self` |
| table not found | `table_not_found` |
| game already started | `game_started` |
| table is full | `table_full` |
| seat not found | `seat_not_found` |
| only the host sets the rules | `host_only_rules` |
| rules lock once the game starts | `rules_locked` |
| only the host can deal | `host_only_deal` |
| already dealt | `already_dealt` |
| need at least two players | `need_two_players` |
| no round in progress | `no_round` |
| round still running | `round_running` |
| not enough players | `not_enough_players` |
| no such seat | `no_such_seat` |
| player is connected | `player_connected` |
| grace period still running | `grace_running` |

(Reserved for later tasks: `pin_required`, `wrong_pin`, `bad_pin`, `bad_stack`, `rate_limited`.)

- [ ] **Step 1: Replace every error string** in `game.ts` and `rooms.ts` per the table (hand edits; the dynamic `` `answer the +${s.pendingDraw} or draw` `` becomes the static `'answer_pot'`).
- [ ] **Step 2: Run the suite**: `npm test`. Fix any test that asserted prose (search: `grep -rn "error" server/test | grep -i "toBe\|toEqual\|toContain"`) by swapping in the code. `App.tsx`/client rendering keeps working (it shows the raw string until Task 9 localizes it).
- [ ] **Step 3: Typecheck**: `npm run typecheck` — PASS.
- [ ] **Step 4: Commit**

```bash
git add server
git commit -m "refactor: server rejection reasons become stable snake_case codes"
```

---

### Task 3: Engine — strict stacking (`pendingDrawKind`)

**Files:**
- Modify: `server/src/engine/game.ts` (GameState, createGame, play/draw cases, removeFromRound)
- Modify: `server/src/engine/views.ts`, `shared/src/types.ts` (RoomStateView)
- Test: `server/test/game-rules.test.ts`

**Interfaces:**
- Consumes: `fixedState(hands, top, {rules})` and `card(color, value)` helpers from `server/test/game-play.test.js`.
- Produces: `GameState.pendingDrawKind: 'draw2' | 'wild4' | null`; `RoomStateView.pendingDrawKind: 'draw2' | 'wild4' | null` (Task 13's client `canPlay` reads it).

- [ ] **Step 1: Write the failing tests** (append to `game-rules.test.ts`)

```ts
describe('strict stacking: +2 answers +2, +4 answers +4', () => {
  test('a wild4 may NOT answer a +2 pot', () => {
    const d2 = card('red', 'draw2');
    const w4 = card(null, 'wild4');
    const s = fixedState(
      [[d2, card('red', '1')], [w4, card('green', '2')], [card('blue', '3')]],
      card('red', '7'), { rules: STACK });
    const r1 = applyAction(s, { type: 'play', seat: 0, cardId: d2.id });
    if (!r1.ok) throw new Error(r1.error);
    const r2 = applyAction(r1.state, { type: 'play', seat: 1, cardId: w4.id, chosenColor: 'green' });
    expect(r2).toEqual({ ok: false, error: 'answer_pot' });
  });
  test('a draw2 may NOT answer a +4 pot', () => {
    const w4 = card(null, 'wild4');
    const d2 = card('blue', 'draw2');
    const s = fixedState(
      [[w4, card('red', '1')], [d2, card('green', '2')], [card('blue', '3')]],
      card('red', '7'), { rules: STACK });
    const r1 = applyAction(s, { type: 'play', seat: 0, cardId: w4.id, chosenColor: 'red' });
    if (!r1.ok) throw new Error(r1.error);
    expect(applyAction(r1.state, { type: 'play', seat: 1, cardId: d2.id }).ok).toBe(false);
  });
  test('kind resets when the pot is taken', () => {
    const d2 = card('red', 'draw2');
    const s = fixedState(
      [[d2, card('red', '1')], [card('green', '2')], [card('blue', '3')]],
      card('red', '7'), { rules: STACK });
    const r1 = applyAction(s, { type: 'play', seat: 0, cardId: d2.id });
    if (!r1.ok) throw new Error(r1.error);
    const r2 = applyAction(r1.state, { type: 'draw', seat: 1 });
    if (!r2.ok) throw new Error(r2.error);
    expect(r2.state.pendingDrawKind).toBeNull();
  });
});
```

Note: the tests still use `cardId:` here — the `cardIds` migration happens in Task 4.

- [ ] **Step 2: Run — expect FAIL** (wild4 currently answers a +2 pot): `npx vitest run test/game-rules.test.ts -r server`
- [ ] **Step 3: Implement** in `game.ts`:
  - `GameState` gains `pendingDrawKind: 'draw2' | 'wild4' | null;` — init `null` in `createGame`; the first-flip `draw2`-with-stacking branch sets `state.pendingDrawKind = 'draw2'`.
  - In `play`: `const stackAnswer = s.pendingDraw > 0 && card.value === s.pendingDrawKind;`
  - `draw2` case (stacking): `s.pendingDraw += 2; s.pendingDrawKind = 'draw2';`
  - `wild4` case (stacking): `s.pendingDraw += 4; s.pendingDrawKind = 'wild4';`
  - `draw` pot-take branch and `removeFromRound` pot reset: also `s.pendingDrawKind = null;`
  - `shared/src/types.ts` `RoomStateView`: add `pendingDrawKind: 'draw2' | 'wild4' | null;` after `pendingDraw`; `views.ts` `projectView`: `pendingDrawKind: g ? g.pendingDrawKind : null,`.
- [ ] **Step 4: Update the one flipped legacy test** — `game-rules.test.ts` “a wild4 stacks on a draw2 and sets the chosen colour (pot 6)” now contradicts strict stacking: rewrite it as “a second draw2 stacks (pot 4), colour follows the answer card” using two `draw2` cards, expecting `pendingDraw` 4.
- [ ] **Step 5: Run full suite + typecheck — PASS**: `npm test && npm run typecheck`
- [ ] **Step 6: Commit**: `git add shared server && git commit -m "feat: strict same-kind stacking with pendingDrawKind"`

---

### Task 4: Engine — `cardIds` migration + stack discard (`multiDiscard`)

**Files:**
- Modify: `server/src/engine/game.ts` (Action, play case), `server/src/sockets.ts:56` (adapter), `shared/src/types.ts` (Effect)
- Modify: `server/test/*.test.ts` (mechanical), `server/src/rooms.ts` (no change expected — `SeatlessAction` adapts)
- Test: create `server/test/game-multidiscard.test.ts`

**Interfaces:**
- Consumes: `fixedState`/`card` helpers; strict-stacking fields from Task 3.
- Produces: `Action` play variant `{ type: 'play'; seat: number; cardIds: number[]; chosenColor?: Color }`; `Effect` played variant `{ type: 'played'; seat: number; cards: Card[] }`. Task 8 exposes `playCards` over the wire; until then `sockets.ts` wraps the old event: `{ type: 'play', cardIds: [p.cardId], chosenColor: p.chosenColor }`.

- [ ] **Step 1: Mechanical migration** of the action and effect shapes:

```bash
perl -pi -e "s/type: 'play', seat: (\w+), cardId: ([\w.]+)/type: 'play', seat: \1, cardIds: [\2]/g" server/test/*.test.ts
perl -pi -e "s/\{ type: 'played', seat: ([\w.]+), card: ([\w.]+) \}/{ type: 'played', seat: \1, cards: [\2] }/g" server/test/*.test.ts
```

Then in `game.ts`: change the Action variant to `{ type: 'play'; seat: number; cardIds: number[]; chosenColor?: Color }`; in `shared/src/types.ts` change the Effect variant to `{ type: 'played'; seat: number; cards: Card[] }`. In `sockets.ts:56` wrap: `store.act(..., { type: 'play', cardIds: [p.cardId], chosenColor: p.chosenColor })`.

Inside the `play` case, bind `const [firstId] = action.cardIds;` and keep single-card logic against `firstId` for now; emit `effects.push({ type: 'played', seat: action.seat, cards: [card] })`. Grep for stragglers: `grep -rn "cardId" server/src server/test | grep -v cardIds | grep -v pendingDrawn` (the `pendingDrawn.cardId` field keeps its name).

- [ ] **Step 2: Run suite + typecheck — must be green before the feature**: `npm test && npm run typecheck`
- [ ] **Step 3: Write the failing multiDiscard tests**

```ts
// server/test/game-multidiscard.test.ts
import { describe, expect, test } from 'vitest';
import { applyAction } from '../src/engine/game.js';
import { card, fixedState } from './game-play.test.js';

const MULTI = { stacking: false, forcePlay: false, drawToMatch: false, multiDiscard: true };
const CLASSIC = { ...MULTI, multiDiscard: false };

describe('house rule: stack discard (same-value numbers)', () => {
  test('three fives go down together; the last one sets the color', () => {
    const b5 = card('blue', '5'); const r5 = card('red', '5'); const g5 = card('green', '5');
    const s = fixedState(
      [[b5, r5, g5, card('yellow', '1')], [card('green', '2')], [card('blue', '3')]],
      card('blue', '7'), { rules: MULTI });
    const r = applyAction(s, { type: 'play', seat: 0, cardIds: [b5.id, r5.id, g5.id] });
    if (!r.ok) throw new Error(r.error);
    expect(r.state.players[0]!.hand).toHaveLength(1);
    expect(r.state.discard.slice(-3).map((c) => c.id)).toEqual([b5.id, r5.id, g5.id]);
    expect(r.state.currentColor).toBe('green');
    expect(r.state.turn).toBe(1);
    expect(r.effects).toContainEqual({ type: 'played', seat: 0, cards: [b5, r5, g5] });
  });
  test('first card must be playable', () => {
    const r5 = card('red', '5'); const g5 = card('green', '5');
    const s = fixedState(
      [[r5, g5, card('blue', '1')], [card('green', '2')]],
      card('blue', '7'), { rules: MULTI });
    expect(applyAction(s, { type: 'play', seat: 0, cardIds: [r5.id, g5.id] }).ok).toBe(false);
  });
  test('mixed values and action cards are rejected as bad_stack', () => {
    const b5 = card('blue', '5'); const b6 = card('blue', '6');
    const sk = card('blue', 'skip'); const sk2 = card('red', 'skip');
    const s = fixedState(
      [[b5, b6, sk, sk2], [card('green', '2')]],
      card('blue', '7'), { rules: MULTI });
    expect(applyAction(s, { type: 'play', seat: 0, cardIds: [b5.id, b6.id] }))
      .toEqual({ ok: false, error: 'bad_stack' });
    expect(applyAction(s, { type: 'play', seat: 0, cardIds: [sk.id, sk2.id] }))
      .toEqual({ ok: false, error: 'bad_stack' });
  });
  test('with the rule off, two cards are rejected', () => {
    const b5 = card('blue', '5'); const r5 = card('red', '5');
    const s = fixedState([[b5, r5], [card('green', '2')]], card('blue', '7'), { rules: CLASSIC });
    expect(applyAction(s, { type: 'play', seat: 0, cardIds: [b5.id, r5.id] }))
      .toEqual({ ok: false, error: 'bad_stack' });
  });
  test('emptying the hand with a stack wins the round', () => {
    const b5 = card('blue', '5'); const r5 = card('red', '5');
    const s = fixedState([[b5, r5], [card('green', '2')]], card('blue', '7'), { rules: MULTI });
    const r = applyAction(s, { type: 'play', seat: 0, cardIds: [b5.id, r5.id] });
    if (!r.ok) throw new Error(r.error);
    expect(r.state.winner).toBe(0);
  });
  test('one card left after a stack arms the catch window', () => {
    const b5 = card('blue', '5'); const r5 = card('red', '5');
    const s = fixedState(
      [[b5, r5, card('yellow', '9')], [card('green', '2')]],
      card('blue', '7'), { rules: MULTI });
    const r = applyAction(s, { type: 'play', seat: 0, cardIds: [b5.id, r5.id] });
    if (!r.ok) throw new Error(r.error);
    expect(r.state.catchWindow).toEqual({ seat: 0 });
  });
  test('duplicate ids in one stack are rejected', () => {
    const b5 = card('blue', '5');
    const s = fixedState([[b5, card('red', '5')], [card('green', '2')]], card('blue', '7'), { rules: MULTI });
    expect(applyAction(s, { type: 'play', seat: 0, cardIds: [b5.id, b5.id] }).ok).toBe(false);
  });
});
```

- [ ] **Step 4: Run — expect FAIL**: `npx vitest run test/game-multidiscard.test.ts -r server`
- [ ] **Step 5: Implement** the `play` case around the existing single-card path:

```ts
case 'play': {
  if (s.turn !== action.seat) return err('not_your_turn');
  if (s.mustChooseColor) return err('choose_color_first');
  if (s.pendingDrawn && s.pendingDrawn.seat === action.seat
      && (action.cardIds.length !== 1 || s.pendingDrawn.cardId !== action.cardIds[0]))
    return err('play_drawn_or_pass');
  if (action.cardIds.length === 0) return err('card_not_in_hand');
  if (new Set(action.cardIds).size !== action.cardIds.length) return err('bad_stack');
  const cards = action.cardIds.map((id) => player.hand.find((c) => c.id === id));
  if (cards.some((c) => !c)) return err('card_not_in_hand');
  const stack = cards as Card[];
  const card = stack[0]!;
  const isNumber = (c: Card) => /^\d$/.test(c.value);
  if (stack.length > 1) {
    if (!s.rules.multiDiscard) return err('bad_stack');
    if (s.pendingDraw > 0) return err('answer_pot');
    if (!stack.every((c) => isNumber(c) && c.value === card.value)) return err('bad_stack');
  }
  const top = s.discard.at(-1)!;
  const stackAnswer = s.pendingDraw > 0 && card.value === s.pendingDrawKind;
  if (s.pendingDraw > 0 && !stackAnswer) return err('answer_pot');
  if (!stackAnswer && !isPlayable(card, top, s.currentColor)) return err('card_no_match');
  const isWild = card.value === 'wild' || card.value === 'wild4';
  if (isWild && !action.chosenColor) return err('wild_needs_color');

  s.catchWindow = null;
  s.pendingDrawn = null;
  for (const c of stack) player.hand.splice(player.hand.findIndex((h) => h.id === c.id), 1);
  s.discard.push(...stack);
  const last = stack.at(-1)!;
  s.currentColor = isWild ? action.chosenColor! : last.color;
  effects.push({ type: 'played', seat: action.seat, cards: stack });
  // …win check and catch-window arm stay as they are; then:
  // if (stack.length > 1) { s.turn = nextSeat(s, action.seat); return { ok: true, state: s, effects }; }
  // …and the existing per-value switch runs on `card` for single plays.
```

- [ ] **Step 6: Run full suite + typecheck — PASS**: `npm test && npm run typecheck`
- [ ] **Step 7: Commit**: `git add shared server && git commit -m "feat: stack discard of same-value numbers; play takes cardIds"`

---

### Task 5: Engine — draw to match

**Files:**
- Modify: `server/src/engine/game.ts` (`draw` case, non-pot branch)
- Test: create `server/test/game-drawtomatch.test.ts`

**Interfaces:**
- Consumes: Task 3/4 state fields.
- Produces: no new types — behavior only. `effects` still carry one `{ type: 'drew', seat, count }` with the total drawn.

- [ ] **Step 1: Write the failing tests**

```ts
// server/test/game-drawtomatch.test.ts
import { describe, expect, test } from 'vitest';
import { applyAction } from '../src/engine/game.js';
import { card, fixedState } from './game-play.test.js';

const DTM = { stacking: false, forcePlay: false, drawToMatch: true, multiDiscard: false };
const DTM_FORCE = { ...DTM, forcePlay: true };

describe('house rule: draw to match', () => {
  test('draws past unplayable cards and stops at the first playable', () => {
    const s = fixedState([[card('blue', '1')], [card('green', '2')]], card('red', '7'), { rules: DTM });
    const hit = card('red', '9');
    s.drawPile = [hit, card('green', '4'), card('blue', '3')]; // pop() order: blue3, green4, red9
    const r = applyAction(s, { type: 'draw', seat: 0 });
    if (!r.ok) throw new Error(r.error);
    expect(r.state.players[0]!.hand).toHaveLength(4); // 1 + 3 drawn
    expect(r.effects).toContainEqual({ type: 'drew', seat: 0, count: 3 });
    expect(r.state.pendingDrawn).toEqual({ seat: 0, cardId: hit.id }); // play-or-pass (forcePlay off)
    expect(r.state.turn).toBe(0);
  });
  test('with forcePlay the found card goes straight down', () => {
    const s = fixedState([[card('blue', '1')], [card('green', '2')]], card('red', '7'), { rules: DTM_FORCE });
    const hit = card('red', '9');
    s.drawPile = [hit, card('blue', '3')];
    const r = applyAction(s, { type: 'draw', seat: 0 });
    if (!r.ok) throw new Error(r.error);
    expect(r.state.discard.at(-1)!.id).toBe(hit.id);
    expect(r.state.turn).toBe(1);
  });
  test('penalty pots still draw exact counts', () => {
    const d2 = card('red', 'draw2');
    const s = fixedState(
      [[d2, card('red', '1')], [card('green', '2')], [card('blue', '3')]],
      card('red', '7'), { rules: { ...DTM, stacking: true } });
    const r1 = applyAction(s, { type: 'play', seat: 0, cardIds: [d2.id] });
    if (!r1.ok) throw new Error(r1.error);
    const r2 = applyAction(r1.state, { type: 'draw', seat: 1 });
    if (!r2.ok) throw new Error(r2.error);
    expect(r2.state.players[1]!.hand).toHaveLength(3); // exactly 2 drawn, no draw-to-match
  });
  test('both piles dry: drawing stops and the turn passes', () => {
    const s = fixedState([[card('blue', '1')], [card('green', '2')]], card('red', '7'), { rules: DTM });
    s.drawPile = [card('blue', '3')]; // one unplayable card, then nothing (discard has only its top)
    const r = applyAction(s, { type: 'draw', seat: 0 });
    if (!r.ok) throw new Error(r.error);
    expect(r.state.players[0]!.hand).toHaveLength(2);
    expect(r.state.turn).toBe(1);
    expect(r.state.pendingDrawn).toBeNull();
  });
});
```

- [ ] **Step 2: Run — expect FAIL**: `npx vitest run test/game-drawtomatch.test.ts -r server`
- [ ] **Step 3: Implement** — replace the single-draw block in the `draw` case (after the pot branch):

```ts
let total = 0;
let drawnCard: Card | null = null;
for (;;) {
  const n = drawFromPile(s, action.seat, 1);
  if (n === 0) { drawnCard = null; break; }
  total += 1;
  drawnCard = player.hand.at(-1)!;
  const playable = isPlayable(drawnCard, s.discard.at(-1)!, s.currentColor);
  if (playable || !s.rules.drawToMatch) break;
}
effects.push({ type: 'drew', seat: action.seat, count: total });
if (!drawnCard || !isPlayable(drawnCard, s.discard.at(-1)!, s.currentColor)) {
  s.turn = nextSeat(s, action.seat);
  return { ok: true, state: s, effects };
}
// existing forcePlay / pendingDrawn branch continues here unchanged,
// with `drawnCard` in place of the old `player.hand.at(-1)!`
```

Note the recursive force-play call now sends `cardIds: [drawnCard.id]`.

- [ ] **Step 4: Run full suite + typecheck — PASS**: `npm test && npm run typecheck`
- [ ] **Step 5: Commit**: `git add server && git commit -m "feat: draw-to-match house rule"`

---

### Task 6: Rooms — 5-character codes + PIN

**Files:**
- Modify: `server/src/rooms.ts` (ALPHABET, createRoom, norm, join, new setPin, viewFor), `server/src/engine/views.ts`, `shared/src/types.ts` (RoomStateView)
- Test: `server/test/rooms.test.ts`

**Interfaces:**
- Consumes: `sanitizeRules` (Task 1), error codes (Task 2).
- Produces: `createRoom(opts?: { seed?: number; rules?: Partial<Rules>; pin?: string | null })`; `join(code, name, pin?: string)` returning `pin_required` / `wrong_pin`; `setPin(code, token, pin: string | null)` (`bad_pin` on format violation, host+lobby only); `RoomStateView.hasPin: boolean` and `RoomStateView.pin: string | null` (actual PIN only in the host's view; `null` for everyone else). `Room.code` is now 5 chars, unhyphenated.

- [ ] **Step 1: Write the failing tests** (append to `rooms.test.ts`)

```ts
describe('5-char codes and room PIN', () => {
  test('codes are 5 chars from the safe alphabet, no hyphen', () => {
    const room = new RoomStore().createRoom();
    expect(room.code).toMatch(/^[34679ACDEFHKMNPRTWXY]{5}$/);
  });
  test('lookup strips spaces and dashes, case-insensitive', () => {
    const store = new RoomStore();
    const room = store.createRoom();
    const messy = `${room.code.toLowerCase().slice(0, 2)}-${room.code.slice(2)}`;
    expect(store.getRoom(messy)).toBe(room);
  });
  test('createRoom applies rules and pin', () => {
    const store = new RoomStore();
    const room = store.createRoom({ rules: { multiDiscard: true }, pin: '1234' });
    expect(room.rules.multiDiscard).toBe(true);
    expect(room.pin).toBe('1234');
  });
  test('join without pin → pin_required; wrong pin → wrong_pin; right pin seats you', () => {
    const store = new RoomStore();
    const room = store.createRoom({ pin: '1234' });
    expect(store.join(room.code, 'Ann')).toEqual({ ok: false, error: 'pin_required' });
    expect(store.join(room.code, 'Ann', '0000')).toEqual({ ok: false, error: 'wrong_pin' });
    expect(store.join(room.code, 'Ann', '1234').ok).toBe(true);
  });
  test('resume by token bypasses the pin', () => {
    const store = new RoomStore();
    const room = store.createRoom({ pin: '1234' });
    const j = store.join(room.code, 'Ann', '1234');
    if (!j.ok) throw new Error(j.error);
    expect(store.resume(room.code, j.token).ok).toBe(true);
  });
  test('setPin: host-only, lobby-only, format-checked, removable', () => {
    const store = new RoomStore();
    const room = store.createRoom();
    const host = store.join(room.code, 'Host');
    const guest = store.join(room.code, 'Guest');
    if (!host.ok || !guest.ok) throw new Error('setup');
    expect(store.setPin(room.code, guest.token, '1234')).toEqual({ ok: false, error: 'host_only_rules' });
    expect(store.setPin(room.code, host.token, '12x4')).toEqual({ ok: false, error: 'bad_pin' });
    expect(store.setPin(room.code, host.token, '4321').ok).toBe(true);
    expect(room.pin).toBe('4321');
    expect(store.setPin(room.code, host.token, null).ok).toBe(true);
    expect(room.pin).toBeNull();
    store.startGame(room.code, host.token);
    expect(store.setPin(room.code, host.token, '1234')).toEqual({ ok: false, error: 'rules_locked' });
  });
  test('only the host view carries the pin; everyone sees hasPin', () => {
    const store = new RoomStore();
    const room = store.createRoom({ pin: '1234' });
    store.join(room.code, 'Host', '1234');
    store.join(room.code, 'Guest', '1234');
    expect(store.viewFor(room.code, 0).pin).toBe('1234');
    expect(store.viewFor(room.code, 0).hasPin).toBe(true);
    expect(store.viewFor(room.code, 1).pin).toBeNull();
    expect(store.viewFor(room.code, 1).hasPin).toBe(true);
  });
});
```

Existing `rooms.test.ts` assertions on the `XXXX-XXXX` shape (search for `-` in code expectations) must be updated to the new 5-char shape in this task.

- [ ] **Step 2: Run — expect FAIL**: `npx vitest run test/rooms.test.ts -r server`
- [ ] **Step 3: Implement** in `rooms.ts`:

```ts
const ALPHABET = '34679ACDEFHKMNPRTWXY'; // no look-alikes (0/O, 1/I/L, 5/S, 8/B, 2/Z) or sound-alikes (U/V, G/J)
const CODE_LEN = 5;
const norm = (code: string) => code.toUpperCase().replace(/[\s-]/g, '');
```

`Room` gains `pin: string | null;`. `createRoom(opts: { seed?: number; rules?: Partial<Rules>; pin?: string | null } = {})` generates `key` of `CODE_LEN` chars, sets `code: key` (no hyphen), `rules: sanitizeRules(opts.rules)`, `pin: opts.pin ?? null`.

`join(code, name, pin?: string)` — after the phase/full checks, before seating:

```ts
if (room.pin !== null && pin !== room.pin) {
  return { ok: false as const, error: pin === undefined ? 'pin_required' : 'wrong_pin' };
}
```

`setPin` mirrors `setRules` (same host/lobby guards, reusing `host_only_rules` / `rules_locked`), plus:

```ts
if (pin !== null && !/^\d{4}$/.test(pin)) return { ok: false as const, error: 'bad_pin' };
room.pin = pin;
```

`viewFor` passes `hasPin: room.pin !== null` and `pin: seat === room.hostSeat ? room.pin : null` through `ViewContext` into `projectView`; add both fields to `RoomStateView` in `shared/src/types.ts` and to `ViewContext`/`projectView` in `views.ts`.

- [ ] **Step 4: Run full suite + typecheck — PASS**: `npm test && npm run typecheck`
- [ ] **Step 5: Commit**: `git add shared server && git commit -m "feat: 5-char room codes and optional 4-digit room PIN"`

---

### Task 7: Rate limiter + HTTP wiring

**Files:**
- Create: `server/src/limiter.ts`
- Modify: `server/src/server.ts` (POST body, 429, limits plumbing), `server/src/sockets.ts` (accept the parameter)
- Test: create `server/test/limiter.test.ts`

**Interfaces:**
- Produces: `class RateLimiter { constructor(max: number, windowMs: number, now?: () => number); allow(key: string): boolean; blocked(key: string): boolean; hit(key: string): void; sweep(): void }`; `interface ServerLimits { create: RateLimiter; join: RateLimiter; pin: RateLimiter }`, `defaultLimits(): ServerLimits` (10/min, 20/min, 5/min), and `buildServer(store?, limits?: ServerLimits)`. `POST /api/rooms` accepts `{ rules?: Partial<Rules>; pin?: string }` (invalid pin format → ignored, room created open) and answers 429 `{ error: 'rate_limited' }` over the limit.

- [ ] **Step 1: Write the failing tests**

```ts
// server/test/limiter.test.ts
import { describe, expect, test } from 'vitest';
import { RateLimiter } from '../src/limiter.js';

describe('fixed-window rate limiter', () => {
  test('allows max hits per window, then blocks', () => {
    let t = 0;
    const lim = new RateLimiter(3, 60_000, () => t);
    expect(lim.allow('a')).toBe(true);
    expect(lim.allow('a')).toBe(true);
    expect(lim.allow('a')).toBe(true);
    expect(lim.allow('a')).toBe(false);
    expect(lim.allow('b')).toBe(true); // keys are independent
  });
  test('window expiry resets the count', () => {
    let t = 0;
    const lim = new RateLimiter(1, 60_000, () => t);
    expect(lim.allow('a')).toBe(true);
    expect(lim.allow('a')).toBe(false);
    t = 60_001;
    expect(lim.allow('a')).toBe(true);
  });
  test('blocked() peeks without counting; hit() counts without answering', () => {
    let t = 0;
    const lim = new RateLimiter(2, 60_000, () => t);
    expect(lim.blocked('a')).toBe(false);
    lim.hit('a'); lim.hit('a');
    expect(lim.blocked('a')).toBe(true);
  });
});
```

- [ ] **Step 2: Run — expect FAIL**: `npx vitest run test/limiter.test.ts -r server`
- [ ] **Step 3: Implement** `server/src/limiter.ts`:

```ts
export class RateLimiter {
  private windows = new Map<string, { start: number; count: number }>();
  constructor(
    private max: number,
    private windowMs: number,
    private now: () => number = Date.now,
  ) {}
  blocked(key: string): boolean {
    const w = this.windows.get(key);
    return !!w && this.now() - w.start < this.windowMs && w.count >= this.max;
  }
  hit(key: string): void {
    const t = this.now();
    const w = this.windows.get(key);
    if (!w || t - w.start >= this.windowMs) this.windows.set(key, { start: t, count: 1 });
    else w.count += 1;
  }
  allow(key: string): boolean {
    if (this.blocked(key)) return false;
    this.hit(key);
    return true;
  }
  sweep(): void {
    const t = this.now();
    for (const [k, w] of this.windows) if (t - w.start >= this.windowMs) this.windows.delete(k);
  }
}
```

In `server.ts`:

```ts
import { RateLimiter } from './limiter.js';
import { sanitizeRules, type Rules } from '@uno/shared';

export interface ServerLimits { create: RateLimiter; join: RateLimiter; pin: RateLimiter }
export const defaultLimits = (): ServerLimits => ({
  create: new RateLimiter(10, 60_000),
  join: new RateLimiter(20, 60_000),
  pin: new RateLimiter(5, 60_000),
});

export async function buildServer(store = new RoomStore(), limits: ServerLimits = defaultLimits()) {
  const app = Fastify();
  app.post('/api/rooms', async (req, reply) => {
    if (!limits.create.allow(req.ip)) return reply.code(429).send({ error: 'rate_limited' });
    const body = (req.body ?? {}) as { rules?: Partial<Rules>; pin?: string };
    const pin = typeof body.pin === 'string' && /^\d{4}$/.test(body.pin) ? body.pin : null;
    const room = store.createRoom({ rules: sanitizeRules(body.rules), pin });
    return { code: room.code };
  });
  // … static + sockets as before, but: attachSockets(io, store, limits);
```

The sweep interval in the main block also calls `limits.create.sweep(); limits.join.sweep(); limits.pin.sweep();`.
`attachSockets` gains the third parameter now but ignores it until Task 8 (name it `_limits: ServerLimits`) so this task compiles standalone.
Note: `POST /api/rooms` with no body and no content-type must keep working (the Landing may send nothing) — `req.body ?? {}` covers it because Fastify only parses a body when the content-type header is present.

- [ ] **Step 4: Run full suite + typecheck — PASS**: `npm test && npm run typecheck`
- [ ] **Step 5: Commit**: `git add server && git commit -m "feat: in-memory rate limiting and rules/pin on room creation"`

---

### Task 8: Protocol — `playCards`, `setPin`, PIN-aware `joinRoom`

**Files:**
- Modify: `shared/src/types.ts` (ClientToServerEvents), `server/src/sockets.ts`, `client/src/store.tsx`, `client/src/screens/Table.tsx` (play call sites)
- Test: `server/test/integration.test.ts`

**Interfaces:**
- Consumes: engine `cardIds` (Task 4), rooms PIN (Task 6), limits (Task 7).
- Produces (wire shapes the client tasks rely on):
  - `playCards: (p: { cardIds: number[]; chosenColor?: Color }) => void` (replaces `playCard`)
  - `setPin: (p: { pin: string | null }) => void`
  - `joinRoom` payload gains `pin?: string`
  - store: `actions.play(cardIds: number[], chosenColor?: Color)`, `actions.setPin(pin: string | null)`, `join(code, name?, pin?)`; `Store` gains `joinError: string | null` (transient join failures such as `pin_required`/`wrong_pin`/`rate_limited`) while fatal `table_not_found` keeps driving the not-found screen via the existing `error`.

- [ ] **Step 1: Update shared types.** In `ClientToServerEvents`: replace `playCard` with `playCards: (p: { cardIds: number[]; chosenColor?: Color }) => void;` and add `setPin: (p: { pin: string | null }) => void;`; `joinRoom` payload becomes `{ code: string; name?: string; token?: string; pin?: string }`.
- [ ] **Step 2: Rewire `sockets.ts`.** `attachSockets(io, store, limits)` uses them:

```ts
socket.on('joinRoom', (p, ack) => {
  const ip = socket.handshake.address;
  if (!limits.join.allow(ip)) return ack({ ok: false, error: 'rate_limited' });
  const existing = p.token ? store.resume(p.code, p.token) : { ok: false as const, error: '' };
  let joined;
  if (existing.ok) {
    joined = { ok: true as const, seat: existing.seat, token: p.token! };
  } else {
    const pinKey = `${ip}:${p.code.toUpperCase()}`;
    if (limits.pin.blocked(pinKey)) return ack({ ok: false, error: 'rate_limited' });
    joined = store.join(p.code, p.name ?? 'Player', p.pin);
    if (!joined.ok && joined.error === 'wrong_pin') limits.pin.hit(pinKey);
  }
  if (!joined.ok) return ack({ ok: false, error: joined.error });
  // … unchanged: socket.data, setConnection, ack with roomName, broadcast
});
socket.on('playCards', (p) => handle(() => store.act(seatOf().code, seatOf().token, { type: 'play', cardIds: p.cardIds, chosenColor: p.chosenColor })));
socket.on('setPin', (p) => handle(() => store.setPin(seatOf().code, seatOf().token, p.pin)));
```

Delete the old `playCard` handler.

- [ ] **Step 3: Update the client store** (`client/src/store.tsx`) so the workspace still typechecks:
  - `play: (cardIds, chosenColor) => socket.emit('playCards', { cardIds, chosenColor })` with signature `play(cardIds: number[], chosenColor?: Color)`;
  - `setPin: (pin) => socket.emit('setPin', { pin })`;
  - `join(code, name?, pin?)` forwards `pin`; on a failed ack, route `pin_required`, `wrong_pin` and `rate_limited` into new state `joinError: string | null` (exposed on the Store interface) instead of the fatal `error`.
  - Call sites in `Table.tsx`: `actions.play(c.id)` → `actions.play([c.id])`, `actions.play(wildCardId, c)` → `actions.play([wildCardId], c)`, `actions.play(forcedWildId, c)` → `actions.play([forcedWildId], c)`.
- [ ] **Step 4: Update the socket integration test.** In `integration.test.ts`, replace every `emit('playCard', { cardId: X` with `emit('playCards', { cardIds: [X]` (rest of the payload unchanged). Add one PIN round-trip test, matching the file's existing server-boot/client helpers and imports:

```ts
test('pin flow over sockets: required, wrong, right', async () => {
  const store = new RoomStore();
  const { app, io } = await buildServer(store);
  await app.listen({ port: 0 });
  const port = (app.server.address() as { port: number }).port;
  const room = store.createRoom({ pin: '1234' });
  const c = ioc(`http://localhost:${port}`);
  const join = (payload: object) =>
    new Promise<{ ok: boolean; error?: string }>((res) => c.emit('joinRoom', { code: room.code, ...payload }, res));
  expect((await join({ name: 'Ann' })).error).toBe('pin_required');
  expect((await join({ name: 'Ann', pin: '0000' })).error).toBe('wrong_pin');
  expect((await join({ name: 'Ann', pin: '1234' })).ok).toBe(true);
  c.close(); io.close(); await app.close();
});
```

- [ ] **Step 5: Run full suite + typecheck + build — PASS**: `npm test && npm run typecheck && npm run build`
- [ ] **Step 6: Commit**: `git add shared server client && git commit -m "feat: playCards/setPin protocol and PIN-aware join with rate limits"`

---

### Task 9: Client — i18n foundation, App + Landing localized

**Files:**
- Create: `client/src/i18n.tsx`
- Modify: `client/src/main.tsx` (LocaleProvider), `client/src/App.tsx`, `client/src/screens/Landing.tsx`, `client/src/game.css` (switcher pill style)

**Interfaces:**
- Produces: `type Locale = 'ru' | 'en'`; `LocaleProvider`; hook `useT(): { t(key, vars?): string; terr(code: string): string; locale: Locale; setLocale(l: Locale): void }`; component `LangSwitcher`. Every later client task uses these and may rely on any key listed here. Keys of the form `err.<code>` cover the full Task 2 table plus `pin_required`, `wrong_pin`, `bad_pin`, `bad_stack`, `rate_limited`.

- [ ] **Step 1: Create `client/src/i18n.tsx`** — the complete dictionary (both locales; `{x}` placeholders interpolated by `t`):

```tsx
import { createContext, useContext, useMemo, useState, type ReactNode } from 'react';

export type Locale = 'ru' | 'en';
const STORE_KEY = 'ochre:locale';

const DICT = {
  en: {
    'app.notFoundTitle': 'Table not found',
    'app.notFoundBody': 'The link may have expired — tables close after a while.',
    'app.backToStart': 'Back to start',
    'landing.note': 'No account needed — pick a name and play',
    'landing.h1a': 'Deal a game', 'landing.h1b': 'in ten seconds.',
    'landing.sub': 'Make a room, send the link, deal. Up to four at the table — your rules, laptop or phone.',
    'landing.create': 'Create a room', 'landing.haveInvite': 'I have an invite',
    'landing.joinTitle': 'Join a table',
    'landing.joinSub': 'Type the five-character code your host sent you.',
    'landing.tokenLabel': 'Room code', 'landing.tokenHint': 'Ask your host — five characters, e.g. K7M3X',
    'landing.find': 'Find the table',
    'create.title': 'House rules', 'create.hostTag': 'You’re the host',
    'create.sub': 'Agree these before the first deal. They lock once the game starts.',
    'create.pinLabel': 'Room PIN (optional)', 'create.pinHint': '4 digits — players are asked for it at the door',
    'create.createBtn': 'Create the room',
    'create.linkLabel': 'Invite link', 'create.copy': 'Copy', 'create.copied': 'Copied',
    'create.token': 'Code {code}', 'create.open': 'Open the room',
    'join.title': 'Join the table', 'join.sub': 'Table {code} — pick a name and take a seat.',
    'join.nameLabel': 'Your name at the table', 'join.sit': 'Take a seat',
    'join.pinTitle': 'This table has a PIN', 'join.pinLabel': '4-digit PIN', 'join.pinGo': 'Enter',
    'lobby.tableOf': '{name}’s table', 'lobby.copy': 'Copy invite', 'lobby.copied': 'Copied',
    'lobby.seatOpen': 'Seat open', 'lobby.waiting': 'waiting', 'lobby.away': 'away',
    'lobby.host': 'Host', 'lobby.ready': 'Ready', 'lobby.you': '(you)',
    'lobby.seated': '{n} of 4 seated', 'lobby.canStart': ' · you can start any time',
    'lobby.deal': 'Deal the first hand', 'lobby.waitHost': 'Waiting for the host to deal…',
    'lobby.pinChip': 'PIN {pin}', 'lobby.pinSet': 'Set a PIN', 'lobby.pinRemove': 'Remove PIN',
    'rules.classic': 'Classic rules',
    'table.round': 'Round {n}', 'table.leave': 'Leave',
    'table.left': '{n} left', 'table.cards': '{n} cards',
    'table.bannerWildFlip': 'The flip was wild — pick the colour',
    'table.bannerAnswerYou': 'Answer the +{n} — stack or take it',
    'table.bannerAnswerOther': '{name} must answer +{n}',
    'table.bannerYourTurn': 'Your turn — {color} is live',
    'table.bannerTheirTurn': '{name}’s turn · {color} is live',
    'table.keep': 'Keep it', 'table.draw': 'Draw', 'table.drawMatch': 'Draw until it plays',
    'table.takeN': 'Take +{n}', 'table.catch': 'Catch', 'table.call': 'Call “last card”',
    'table.discardN': 'Discard {n}', 'table.clearPick': 'Clear',
    'table.chooseColour': 'Choose a colour',
    'table.wildFlipSub': 'The flip was wild — you set what plays first.',
    'table.forcedWildSub': 'Force play — your drawn wild goes down.',
    'picker.sub': 'Your wild card sets what plays next.',
    'seat.called': '1 — called it', 'seat.away': ' · away',
    'pause.waiting': 'Waiting for {name}…',
    'pause.body': 'Their seat is held — the game resumes the moment they reopen the link.',
    'pause.continue': 'Continue without them',
    'over.youTake': 'You take it', 'over.takes': '{name} takes it',
    'over.round': 'Round {n}', 'over.youEmptied': 'you emptied your hand first',
    'over.finishWith': 'you finish with {n}',
    'over.out': 'out', 'over.left': '{n} left', 'over.wins': 'wins: {n}',
    'over.again': 'Play again', 'over.leave': 'Leave',
    'color.red': 'red', 'color.yellow': 'yellow', 'color.green': 'green', 'color.blue': 'blue',
    'err.round_over': 'The round is over', 'err.bad_seat': 'Bad seat',
    'err.no_color_pending': 'No colour choice pending', 'err.play_drawn_or_pass': 'Play the drawn card or keep it',
    'err.card_not_in_hand': 'That card is not in your hand', 'err.answer_pot': 'Answer the penalty or take it',
    'err.card_no_match': 'That card does not match', 'err.wild_needs_color': 'Pick a colour for the wild',
    'err.not_your_turn': 'Not your turn', 'err.choose_color_first': 'Choose a colour first',
    'err.nothing_to_pass': 'Nothing to pass', 'err.force_play': 'Force play — the drawn card goes down',
    'err.cannot_call_now': 'You can’t call now', 'err.nothing_to_catch': 'Nothing to catch',
    'err.cannot_catch_self': 'You can’t catch yourself',
    'err.table_not_found': 'Table not found', 'err.game_started': 'The game already started',
    'err.table_full': 'The table is full', 'err.seat_not_found': 'Seat not found',
    'err.host_only_rules': 'Only the host changes this', 'err.rules_locked': 'Locked once the game starts',
    'err.host_only_deal': 'Only the host can deal', 'err.already_dealt': 'Already dealt',
    'err.need_two_players': 'You need at least two players', 'err.no_round': 'No round in progress',
    'err.round_running': 'The round is still running', 'err.not_enough_players': 'Not enough players',
    'err.no_such_seat': 'No such seat', 'err.player_connected': 'That player is connected',
    'err.grace_running': 'Give them a moment — the grace period is running',
    'err.pin_required': 'This table asks for a PIN', 'err.wrong_pin': 'Wrong PIN — try again',
    'err.bad_pin': 'A PIN is exactly 4 digits', 'err.bad_stack': 'Those cards can’t go down together',
    'err.rate_limited': 'Too many attempts — wait a minute',
  },
  ru: {
    'app.notFoundTitle': 'Стол не найден',
    'app.notFoundBody': 'Ссылка могла устареть — столы со временем закрываются.',
    'app.backToStart': 'На главную',
    'landing.note': 'Без аккаунта — назови себя и играй',
    'landing.h1a': 'Раздай игру', 'landing.h1b': 'за десять секунд.',
    'landing.sub': 'Создай комнату, отправь ссылку, раздавай. До четырёх за столом — ваши правила, ноутбук или телефон.',
    'landing.create': 'Создать комнату', 'landing.haveInvite': 'У меня есть код',
    'landing.joinTitle': 'Подключиться к столу',
    'landing.joinSub': 'Введи код из пяти символов от хоста.',
    'landing.tokenLabel': 'Код комнаты', 'landing.tokenHint': 'Спроси у хоста — пять символов, например K7M3X',
    'landing.find': 'Найти стол',
    'create.title': 'Правила стола', 'create.hostTag': 'Ты — хост',
    'create.sub': 'Договоритесь до первой раздачи. После старта правила фиксируются.',
    'create.pinLabel': 'PIN комнаты (необязательно)', 'create.pinHint': '4 цифры — их спросят при входе',
    'create.createBtn': 'Создать комнату',
    'create.linkLabel': 'Ссылка-приглашение', 'create.copy': 'Копировать', 'create.copied': 'Скопировано',
    'create.token': 'Код {code}', 'create.open': 'Войти в комнату',
    'join.title': 'За стол', 'join.sub': 'Стол {code} — представься и садись.',
    'join.nameLabel': 'Твоё имя за столом', 'join.sit': 'Сесть за стол',
    'join.pinTitle': 'У этого стола есть PIN', 'join.pinLabel': 'PIN из 4 цифр', 'join.pinGo': 'Войти',
    'lobby.tableOf': 'Стол {name}', 'lobby.copy': 'Скопировать приглашение', 'lobby.copied': 'Скопировано',
    'lobby.seatOpen': 'Место свободно', 'lobby.waiting': 'ждём', 'lobby.away': 'отошёл',
    'lobby.host': 'Хост', 'lobby.ready': 'Готов', 'lobby.you': '(ты)',
    'lobby.seated': '{n} из 4 за столом', 'lobby.canStart': ' · можно начинать',
    'lobby.deal': 'Раздать первую руку', 'lobby.waitHost': 'Ждём, пока хост раздаст…',
    'lobby.pinChip': 'PIN {pin}', 'lobby.pinSet': 'Поставить PIN', 'lobby.pinRemove': 'Убрать PIN',
    'rules.classic': 'Классика',
    'table.round': 'Раунд {n}', 'table.leave': 'Выйти',
    'table.left': 'осталось {n}', 'table.cards': 'карт: {n}',
    'table.bannerWildFlip': 'Открылась дикая — выбери цвет',
    'table.bannerAnswerYou': 'Ответь на +{n} — переведи или бери',
    'table.bannerAnswerOther': '{name} отвечает на +{n}',
    'table.bannerYourTurn': 'Твой ход — в игре {color}',
    'table.bannerTheirTurn': 'Ход {name} · в игре {color}',
    'table.keep': 'Оставить', 'table.draw': 'Взять', 'table.drawMatch': 'Тянуть до играбельной',
    'table.takeN': 'Взять +{n}', 'table.catch': 'Поймать', 'table.call': 'Крикнуть «последняя!»',
    'table.discardN': 'Сбросить {n}', 'table.clearPick': 'Сбросить выбор',
    'table.chooseColour': 'Выбери цвет',
    'table.wildFlipSub': 'Открылась дикая — ты задаёшь первый цвет.',
    'table.forcedWildSub': 'Форс-плей — вытянутая дикая ложится сразу.',
    'picker.sub': 'Дикая карта задаёт следующий цвет.',
    'seat.called': '1 — крикнул!', 'seat.away': ' · отошёл',
    'pause.waiting': 'Ждём {name}…',
    'pause.body': 'Место сохранено — игра продолжится, как только ссылку откроют снова.',
    'pause.continue': 'Продолжить без них',
    'over.youTake': 'Ты забираешь раунд', 'over.takes': '{name} забирает раунд',
    'over.round': 'Раунд {n}', 'over.youEmptied': 'ты первым опустошил руку',
    'over.finishWith': 'у тебя осталось {n}',
    'over.out': 'вышел', 'over.left': 'осталось {n}', 'over.wins': 'побед: {n}',
    'over.again': 'Ещё раз', 'over.leave': 'Выйти',
    'color.red': 'красный', 'color.yellow': 'жёлтый', 'color.green': 'зелёный', 'color.blue': 'синий',
    'err.round_over': 'Раунд окончен', 'err.bad_seat': 'Нет такого места',
    'err.no_color_pending': 'Выбор цвета не ожидается', 'err.play_drawn_or_pass': 'Сыграй вытянутую карту или оставь её',
    'err.card_not_in_hand': 'Этой карты нет в руке', 'err.answer_pot': 'Ответь на штраф или возьми его',
    'err.card_no_match': 'Карта не подходит', 'err.wild_needs_color': 'Выбери цвет для дикой',
    'err.not_your_turn': 'Не твой ход', 'err.choose_color_first': 'Сначала выбери цвет',
    'err.nothing_to_pass': 'Нечего пропускать', 'err.force_play': 'Форс-плей — вытянутая карта ложится',
    'err.cannot_call_now': 'Сейчас нельзя крикнуть', 'err.nothing_to_catch': 'Ловить нечего',
    'err.cannot_catch_self': 'Себя не поймаешь',
    'err.table_not_found': 'Стол не найден', 'err.game_started': 'Игра уже началась',
    'err.table_full': 'Стол заполнен', 'err.seat_not_found': 'Место не найдено',
    'err.host_only_rules': 'Это меняет только хост', 'err.rules_locked': 'После старта не меняется',
    'err.host_only_deal': 'Раздаёт только хост', 'err.already_dealt': 'Уже раздали',
    'err.need_two_players': 'Нужно хотя бы два игрока', 'err.no_round': 'Раунд не идёт',
    'err.round_running': 'Раунд ещё идёт', 'err.not_enough_players': 'Игроков не хватает',
    'err.no_such_seat': 'Нет такого места', 'err.player_connected': 'Игрок на связи',
    'err.grace_running': 'Подожди — время на возвращение ещё идёт',
    'err.pin_required': 'У стола есть PIN', 'err.wrong_pin': 'Неверный PIN — попробуй ещё',
    'err.bad_pin': 'PIN — это ровно 4 цифры', 'err.bad_stack': 'Эти карты нельзя сбросить вместе',
    'err.rate_limited': 'Слишком много попыток — подожди минуту',
  },
} as const;

export type MsgKey = keyof (typeof DICT)['en'];

export const detectLocale = (): Locale => {
  const stored = localStorage.getItem(STORE_KEY);
  if (stored === 'ru' || stored === 'en') return stored;
  return navigator.language.toLowerCase().startsWith('ru') ? 'ru' : 'en';
};

const Ctx = createContext<{ locale: Locale; setLocale: (l: Locale) => void } | null>(null);

export function LocaleProvider({ children }: { children: ReactNode }) {
  const [locale, setLocaleState] = useState<Locale>(detectLocale);
  const value = useMemo(() => ({
    locale,
    setLocale: (l: Locale) => { localStorage.setItem(STORE_KEY, l); setLocaleState(l); },
  }), [locale]);
  return <Ctx.Provider value={value}>{children}</Ctx.Provider>;
}

export function useT() {
  const ctx = useContext(Ctx);
  if (!ctx) throw new Error('useT outside LocaleProvider');
  const t = (key: MsgKey, vars?: Record<string, string | number>): string => {
    let s: string = DICT[ctx.locale][key] ?? DICT.en[key] ?? key;
    for (const [k, v] of Object.entries(vars ?? {})) s = s.replaceAll(`{${k}}`, String(v));
    return s;
  };
  /** Localize a server rejection code; unknown codes fall back to the raw code. */
  const terr = (code: string): string =>
    (DICT[ctx.locale] as Record<string, string>)[`err.${code}`] ?? code;
  return { t, terr, locale: ctx.locale, setLocale: ctx.setLocale };
}

export function LangSwitcher() {
  const { locale, setLocale } = useT();
  return (
    <button type="button" className="btn btn-ghost lang-switch"
      onClick={() => setLocale(locale === 'ru' ? 'en' : 'ru')}>
      {locale === 'ru' ? 'EN' : 'RU'}
    </button>
  );
}
```

- [ ] **Step 2: Wrap the app.** In `main.tsx`, wrap the existing tree: `<LocaleProvider><StoreProvider><App /></StoreProvider></LocaleProvider>`.
- [ ] **Step 3: Localize `App.tsx` and `Landing.tsx`.** Replace every literal with `t('…')` per the keys above (`landing.h1a`/`h1b` around the `<br />`); App's not-found screen renders `terr(error)` for the body when the code is known, `app.notFoundBody` otherwise. Add `<LangSwitcher />` to the landing header (`landing-top`). Update the join-token placeholder to `K7M3X` and `maxLength={5}`. Add a `.lang-switch` style in `game.css` consistent with `.chip`.
- [ ] **Step 4: Verify**: `npm run typecheck && npm run build` — PASS.
- [ ] **Step 5: Commit**: `git add client && git commit -m "feat: ru/en i18n foundation; landing and app shell localized"`

---

### Task 10: Client — create-room flow with rules, details, and PIN

**Files:**
- Modify: `client/src/screens/Landing.tsx`, `client/src/screens/HostLink.tsx`, `client/src/components/RuleRow.tsx`, `client/src/ui.ts`, `client/src/store.tsx`, `client/src/game.css`

**Interfaces:**
- Consumes: `RULES_CATALOG` from `@uno/shared`; `useT` (Task 9); `POST /api/rooms` body (Task 7).
- Produces: `RuleRow` props become `{ name, desc, details, on, onToggle }` (expandable details); `ui.ts` exports `ruleChips(rules: Rules, locale: Locale): string[]` (catalog-driven, returns `[]` when classic) and drops `RULE_DEFS`/`rulesStashKey`.

- [ ] **Step 1: Rework `HostLink.tsx` into a two-phase screen.** Phase `configure` (no room yet): catalog-driven `RuleRow` list (name = `title[locale]`, desc = `tagline[locale]`, details = `details[locale]`), an optional PIN field (`<input inputMode="numeric" maxLength={4} pattern="[0-9]*">`, label `create.pinLabel`, hint `create.pinHint`), and a `create.createBtn` button doing:

```ts
const res = await fetch('/api/rooms', {
  method: 'POST', headers: { 'content-type': 'application/json' },
  body: JSON.stringify({ rules, pin: pin || undefined }),
});
const body = (await res.json()) as { code: string };
```

Phase `share` (code received): the existing link + `create.token` + `create.open` block, localized. `Landing.tsx` renders `<HostLink />` (no code prop, no fetch of its own) when the user clicks `landing.create`.
- [ ] **Step 2: Delete the stash plumbing.** Remove `rulesStashKey` from `ui.ts` and the sessionStorage block from `store.join` (`store.tsx:74-79`) — rules and PIN now land server-side at creation.
- [ ] **Step 3: Extend `RuleRow`** with a details disclosure: a small `?` button (`aria-expanded`) toggling a `.rulerow-details` paragraph under the row; clicking `?` must not flip the switch (stopPropagation). Style `.rulerow-details` with DS tokens (muted color, `--space-2` padding), matching the design reference.
- [ ] **Step 4: Point `ruleChips` at the catalog**:

```ts
import { RULES_CATALOG, type Rules } from '@uno/shared';
import type { Locale } from './i18n';
export const ruleChips = (rules: Rules, locale: Locale): string[] =>
  RULES_CATALOG.filter((r) => rules[r.id]).map((r) => r.title[locale]);
```

Update the two call sites (`Lobby.tsx`, `Table.tsx`) to pass `locale` and to render a single `t('rules.classic')` chip when the list comes back empty.
- [ ] **Step 5: Verify**: `npm run typecheck && npm run build` — PASS.
- [ ] **Step 6: Commit**: `git add client && git commit -m "feat: create-room screen with catalog rules, details, and PIN"`

---

### Task 11: Client — join flow (5-char code + PIN screen)

**Files:**
- Modify: `client/src/screens/Join.tsx`, `client/src/ui.ts` (fmtCode)

**Interfaces:**
- Consumes: `joinError` codes from the store (Task 8); `useT`.
- Produces: nothing new downstream.

- [ ] **Step 1: Simplify `fmtCode`** to the identity (`export const fmtCode = (code: string): string => code;`) — codes are 5 chars now; keep the export so call sites stand.
- [ ] **Step 2: Rework `Join.tsx`.** Localize all copy (`join.*`). When `joinError` is `pin_required` or `wrong_pin`, render the PIN step instead of the name form: title `join.pinTitle`, numeric input (`inputMode="numeric"`, `maxLength={4}`, autoFocus), an inline `terr('wrong_pin')` message when applicable, and submit `join(code, name, pin)`. Keep the held-seat auto-rejoin effect as is (tokens bypass PINs). The name from the first step persists in state for the PIN retry.
- [ ] **Step 3: Show other transient `joinError`s** (`rate_limited`, `table_full`, `game_started`) under the form via `terr(joinError)` rather than switching to the fatal screen.
- [ ] **Step 4: Verify**: `npm run typecheck && npm run build` — PASS.
- [ ] **Step 5: Commit**: `git add client && git commit -m "feat: five-char join flow with PIN step"`

---

### Task 12: Client — lobby with catalog rules and PIN management

**Files:**
- Modify: `client/src/screens/Lobby.tsx`

**Interfaces:**
- Consumes: `RULES_CATALOG`, `useT`, `actions.setPin`, `view.pin`/`view.hasPin` (Tasks 6/8), extended `RuleRow` (Task 10).

- [ ] **Step 1: Localize the lobby** (`lobby.*` keys; rule chips via `ruleChips(view.rules, locale)` with the `rules.classic` fallback; `lobby.tableOf` with the host name).
- [ ] **Step 2: Host rules panel** — map `RULES_CATALOG` into the extended `RuleRow` (title/tagline/details in the current locale), toggling via the existing `actions.setRules({ ...view.rules, [id]: !view.rules[id] })`.
- [ ] **Step 3: Host PIN row** — beneath the rules: when `view.pin` is set, show `lobby.pinChip` (with the digits) + a `lobby.pinRemove` ghost button (`actions.setPin(null)`); otherwise a 4-digit numeric input + `lobby.pinSet` button (`actions.setPin(value)`). Non-hosts see a plain `PIN` chip only when `view.hasPin`.
- [ ] **Step 4: Verify**: `npm run typecheck && npm run build` — PASS.
- [ ] **Step 5: Commit**: `git add client && git commit -m "feat: lobby rules panel from catalog with PIN management"`

---

### Task 13: Client — table: strict stacking, stack discard, draw-to-match; localize the rest

**Files:**
- Modify: `client/src/screens/Table.tsx`, `client/src/screens/RoundOver.tsx`, `client/src/components/Seat.tsx`, `client/src/components/ColorPicker.tsx`, `client/src/components/PauseOverlay.tsx`, `client/src/game.css`

**Interfaces:**
- Consumes: `view.pendingDrawKind` (Task 3), `actions.play(cardIds, chosenColor?)` (Task 8), `useT`.

- [ ] **Step 1: Strict-stack gating.** In `Table.tsx` `canPlay`, replace the pot branch `c.value === 'draw2' || c.value === 'wild4'` with `c.value === view.pendingDrawKind`. Banner keys `table.bannerAnswerYou`/`table.bannerAnswerOther` take `{n: view.pendingDraw}`.
- [ ] **Step 2: Stack selection.** Add state and handlers:

```tsx
const [picked, setPicked] = useState<number[]>([]);
useEffect(() => { setPicked([]); }, [view.hand.length, view.turnSeat]);
const isNumber = (c: Card) => /^\d$/.test(c.value);
const pickable = (c: Card) =>
  view.rules.multiDiscard && isNumber(c) && view.pendingDraw === 0 && yourTurn &&
  (picked.length === 0
    ? canPlay(c)
    : c.value === view.hand.find((h) => h.id === picked[0])?.value);
const onCardClick = (c: Card) => {
  if (!view.rules.multiDiscard || !isNumber(c)) return playCard(c);
  setPicked((prev) => prev.includes(c.id) ? prev.filter((id) => id !== c.id) : [...prev, c.id]);
};
```

Picked cards render lifted (new `.hand-slot-picked` class in `game.css`, mirroring the raised-card lift from the design reference); a card is clickable when `canPlay(c) || pickable(c)`, routed through `onCardClick`. When `picked.length > 0`, the hand-actions row swaps the draw/keep pair for `table.discardN` (`actions.play(picked); setPicked([])`) and a `table.clearPick` ghost button. Wilds and action cards keep the current tap-to-play path.
- [ ] **Step 3: Draw-to-match label.** The draw button reads `t(view.rules.drawToMatch && view.pendingDraw === 0 ? 'table.drawMatch' : 'table.draw')`; pot-taking keeps `table.takeN` with `{n}`.
- [ ] **Step 4: Localize** every remaining literal: `Table.tsx` (banners with `color: t(\`color.${view.currentColor}\`)`, `table.round`, `table.leave`, `table.left`, `table.cards`, `table.call`, `table.catch`, `table.keep`, colour-picker titles/subtitles), `Seat.tsx` (`seat.called`, `seat.away`), `ColorPicker.tsx` (default subtitle `picker.sub`), `PauseOverlay.tsx` (`pause.*`), `RoundOver.tsx` (`over.*`). The rejection banner shows `terr(rejection)`.
- [ ] **Step 5: Verify**: `npm run typecheck && npm run build` — PASS.
- [ ] **Step 6: Commit**: `git add client && git commit -m "feat: table supports strict stacking, stack discard, draw-to-match; client fully bilingual"`

---

### Task 14: Final verification

**Files:** none new.

- [ ] **Step 1: Full gate**: `npm test && npm run typecheck && npm run build` — all green.
- [ ] **Step 2: Scripted smoke** against the built server on an ephemeral port (NOT :3000): a Node script with two `socket.io-client`s that creates a room via `POST /api/rooms` with `{ rules: { multiDiscard: true, drawToMatch: true, stacking: true }, pin: '1234' }`, verifies `pin_required` → `wrong_pin` → seated, deals, and drives one full turn including a `playCards` call, asserting both views update. Save it as `$CLAUDE_JOB_DIR/tmp/smoke_beta_core.mjs`; it must exit 0.
- [ ] **Step 3: Re-read the spec** (`docs/superpowers/specs/2026-08-12-beta-core-design.md`) section by section and tick off: 4 rule semantics, catalog consumers, protocol shapes, code alphabet, PIN flow, rate limits, i18n, UI changes, error codes, tests. Fix anything missed before declaring done.
- [ ] **Step 4: Commit any stragglers and push**: `git push origin worktree-beta-core-spec`. Remind the user to restart `npm start -w server` after the rebuild (static snapshot) — do not start it for them.

## Out of scope for this plan

Landing promo content (sub-project B), sounds/music (C), rules slide and help (E), metrics/nicknames (D), Timeweb deploy and the security audit (F), voice chat, vibration.
