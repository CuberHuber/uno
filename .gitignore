node_modules/
dist/
dist-tsc/
*.tsbuildinfo
server/public/
.env
.DS_Store
.superpowers/
.remember/
.idea/
# Screen recordings dropped at the repo root; anchored so the hero-loop
# video assets under client/public/ stay tracked.
/*.mov
# Local-only project backlog: the repo is public, unpublished ideas stay
# off GitHub. Entries are mirrored to issues only after explicit approval.
/docs/backlog*.md
.gstack/
# Seam previews from tools/make-music-loop.sh — for listening, never for shipping.
.audio-preview/
# 0x flamegraph capture directories (npm run flame)
*.0x/
# Claude Code: agents and commands under .claude/ are versioned deliberately
# (see docs/superpowers/specs/2026-08-19-devlog-pipeline-design.md), but the
# personal permission list and the worktree checkouts are not.
/.claude/settings.local.json
/.claude/worktrees/
# Local-only media plan: the repo is public, the content strategy is not.
/docs/media-pilot*.md
