node_modules/
dist/
dist-tsc/
*.tsbuildinfo
server/public/
.env
.DS_Store
.superpowers/
.remember/
.idea/
# Screen recordings dropped at the repo root; anchored so the hero-loop
# video assets under client/public/ stay tracked.
/*.mov
.gstack/
# Seam previews from tools/make-music-loop.sh — for listening, never for shipping.
.audio-preview/
