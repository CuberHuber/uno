node_modules/
dist/
dist-tsc/
*.tsbuildinfo
server/public/
.env
.DS_Store
.superpowers/
.remember/
.idea/
# Screen recordings dropped at the repo root; anchored so the hero-loop
# video assets under client/public/ stay tracked.
/*.mov
.gstack/
# 0x flamegraph capture directories (npm run flame)
*.0x/
